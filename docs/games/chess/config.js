/**
 * config.js — إعدادات واجهة الشطرنج
 * يُستورد من أي مكان في chess/
 */

export const CHESS_CONFIG = {

    // ─── أحجام ──────────────────────────────────────────────────────────────
    squareSize: 72,          // حجم المربع الافتراضي (px)
    minSquareSize: 40,       // أصغر حجم (للموبايل الصغير)
    maxSquareSize: 90,       // أكبر حجم (شاشات كبيرة)
    boardPadding: 0,         // padding حول الرقعة

    // ─── ألوان الرقعة ────────────────────────────────────────────────────────
    colors: {
        light:        '#f0d9b5',   // مربع فاتح (كلاسيك)
        dark:         '#b58863',   // مربع داكن
        border:       '#6b3f1f',   // إطار الرقعة

        // إضاءات
        selected:     'rgba(20,  85,  30,  0.55)',   // قطعة مختارة
        validDot:     'rgba(20,  85,  30,  0.35)',   // نقطة حركة ممكنة
        validCapture: 'rgba(20,  85,  30,  0.40)',   // دائرة أكل ممكن
        lastFrom:     'rgba(155, 199,  0,  0.38)',   // من (آخر حركة)
        lastTo:       'rgba(155, 199,  0,  0.38)',   // إلى  (آخر حركة)
        checkSquare:  'rgba(220,  50,  50,  0.65)',  // ملك في كشف

        // إحداثيات (حروف وأرقام على الحافة)
        coordOnLight: '#b58863',
        coordOnDark:  '#f0d9b5',
    },

    // ─── الخط ────────────────────────────────────────────────────────────────
    pieceFont: '"Segoe UI Symbol", "Apple Color Emoji", Arial, sans-serif',

    // ─── التحريك ─────────────────────────────────────────────────────────────
    animation: {
        duration: 180,   // ms
        /** دالة التسارع: ease-in-out */
        easing: t => t < 0.5 ? 2 * t * t : -1 + (4 - 2 * t) * t,
    },

    // ─── إعدادات اللعب ───────────────────────────────────────────────────────
    showCoordinates: true,   // إظهار a-h / 1-8 على حواف الرقعة
    allowDrag: true,         // السماح بالسحب والإفلات
    flipOnBlack: true,       // قلب الرقعة عند دور الأسود في Local (desktop only)
};
